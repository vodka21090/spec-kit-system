# Structure Template

Template for `docs/codebase/STRUCTURE.md` — captures physical file organisation.

**Purpose:** Document where things physically live in the codebase. Answers "where do I put X?". Use real directory names from the repo, not invented ones.

---

## File Template

```markdown
---
schema_version: 1
doc: STRUCTURE
analysis_date: [YYYY-MM-DD]
generated_by: /spec-kit:map-codebase
source_sha: [short-sha]
sections:
  - { id: directory-layout, summary: "[top-level tree]" }
  - { id: directory-purposes, summary: "[what lives in each dir]" }
  - { id: key-file-locations, summary: "[entry points, config, core, tests, docs]" }
  - { id: naming-conventions, summary: "[file/dir naming rules]" }
  - { id: where-to-add-new-code, summary: "[insertion points by change type]" }
  - { id: special-directories, summary: "[generated/build dirs]" }
  - { id: navigation-index, summary: "[path → purpose → doc anchor table]" }
key_files:
  - { path: [path], purpose: [one line], see: ARCHITECTURE#layers }
---

# Codebase Structure

## Directory Layout

[ASCII tree of top-level directories with purpose — use ├── └── │ characters.]

```
[project-root]/
├── [dir]/          # [Purpose]
├── [dir]/          # [Purpose]
└── [file]          # [Purpose]
```

## Directory Purposes

**[Directory Name]:**
- Purpose: [What lives here]
- Contains: [Types of files: e.g., "*.ts source files"]
- Key files: [`path/to/important-file`]
- Subdirectories: [If nested, describe structure]

## Key File Locations

**Entry Points:**
- [`path`]: [Purpose: e.g., "CLI entry point"]

**Configuration:**
- [`path`]: [Purpose: e.g., "TypeScript config"]

**Core Logic:**
- [`path`]: [Purpose: e.g., "Business services"]

**Testing:**
- [`path`]: [Purpose: e.g., "Unit tests"]

**Documentation:**
- [`path`]: [Purpose: e.g., "User-facing docs"]

## Naming Conventions

**Files:**
- [Pattern]: [Example: e.g., "kebab-case.ts for modules"]

**Directories:**
- [Pattern]: [Example: e.g., "kebab-case for feature directories"]

**Special Patterns:**
- [Pattern]: [Example: e.g., "index.ts for directory exports"]

## Where to Add New Code

**New Feature:**
- Primary code: [`directory/path`]
- Tests: [`directory/path`]

**New Component/Module:**
- Implementation: [`directory/path`]
- Tests: [`directory/path`]

**New Route/Command:**
- Definition: [`directory/path`]
- Handler: [`directory/path`]

**Utilities:**
- Shared helpers: [`directory/path`]

## Special Directories

[Any directories with special meaning or generation.]

**[Directory]:**
- Purpose: [e.g., "Generated code", "Build output"]
- Source: [e.g., "Auto-generated by X"]
- Committed: [Yes/No — in .gitignore?]

## Navigation Index

[Inverted index of key dirs/files — module-level, capped. Agents grep this; humans read it as a
per-file table of contents. `see` deep-links into another doc's frozen anchor.]

| Path | Purpose | See |
|------|---------|-----|
| [`src/auth/`] | [Authentication & session] | [ARCHITECTURE#layers] |
| [`src/api/routes.ts`] | [HTTP route declarations] | [ARCHITECTURE#entry-points] |

---

*Structure analysis: [date]. Update when directory structure changes.*
```
